[pytest]
testpaths = tests
filterwarnings =
    # shap 0.52 internally uses deprecated matplotlib colormap APIs; noise
    # originates in the library, not in this codebase.
    ignore::PendingDeprecationWarning:shap
